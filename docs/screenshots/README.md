# Screenshots

Referenced from the root `README.md`. Drop these four images in here with these exact names
and they'll show up automatically (no other edits needed):

| File | What to capture |
|---|---|
| `login.png` | The login page (`/login`) |
| `upload.png` | The upload page (`/`), ideally with a file selected and the disclaimer visible |
| `results.png` | The results view after analyzing an X-ray — label, probability bar, and the **Heatmap** view of the toggle |
| `history.png` | The history dashboard (`/history`) with at least a couple of rows |

PNG or JPG both work. No strict size requirement — a normal browser-window screenshot
(roughly 1200-1600px wide) looks right in the README's two-column layout.
